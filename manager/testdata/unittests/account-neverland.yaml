apiVersion: app.fybrik.io/v1alpha1
kind: FybrikStorageAccount
metadata:
  generateName: account-neverland
  namespace: fybrik-system
spec:
  endpoint: http://s3.eu.cloud-object-storage.appdomain.cloud
  secretRef: credentials-neverland
  regions:
    - neverland

