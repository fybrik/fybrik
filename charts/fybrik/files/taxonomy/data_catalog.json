{
  "type": "object",
  "title": "Common definitions for Data Catalog",
  "definitions": {
    "credentials": {
      "$ref": "#/definitions/vaultPluginPath"
    },
    "details": {
      "type": "object",
      "description": "Asset details",
      "properties": {
        "connection": {
          "$ref": "taxonomy.json#/definitions/Connection"
        },
        "dataFormat": {
          "$ref": "taxonomy.json#/definitions/Dataformat"
        }
      },
      "required": [
        "connection"
      ]
    },
    "resource": {
      "type": "object",
      "description": "AssetMetadata defines model for AssetMetadata.",
      "properties": {
        "columns": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string"
              },
              "tags": {
                "$ref": "taxonomy.json#/definitions/Tags"
              }
            },
            "required": [
              "name"
            ]
          }
        },
        "geography": {
          "type": "string",
          "description": "The geography of the asset"
        },
        "name": {
          "type": "string",
          "description": "Name of the data set"
        },
        "owner": {
          "type": "string"
        },
        "tags": {
          "$ref": "taxonomy.json#/definitions/Tags"
        }
      },
      "required": [
        "name"
      ]
    },
    "vaultPluginPath": {
      "type": "string",
      "description": "This has the vault plugin path where the data credentials will be stored as kubernetes secrets. This value is assumed to be known to the catalog connector. Implements::credentials"
    }
  },
  "$schema": "http://json-schema.org/draft-04/schema#"
}