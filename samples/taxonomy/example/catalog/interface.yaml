definitions:
  Interface:
    oneOf:
      - properties:
          protocol:
            enum: [Kafka]
          dataformat:
            enum: [json, avro]
        required: [protocol, dataformat]
      - properties:
          protocol:
            enum: [S3]
          dataformat:
            enum: [csv, parquet]
        required: [protocol, dataformat]
      - properties:
          protocol:
            enum: [FybrikArrowFlight, DB2]
        required: [protocol]