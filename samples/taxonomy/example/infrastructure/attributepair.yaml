definitions:
  AttributeDefinition:
    oneOf:
      - properties:
          attribute:
            enum: [storage-cost]
          units:
            enum: [US Dollar per TB per month]
        required: [attribute, units]
      - properties:
          attribute:
            enum: [bandwidth]
          units:
            enum: [Mbps, Gbps, Tbps, Kbps, MBps, GBps, TBps, KBps]
        required: [attribute, units]
      - properties:
          attribute:
            enum: [size]
          units:
            enum: [TB, GB, MB, KB]
        required: [attribute, units]
      - properties:
          attribute:
            enum: [distance]
          units:
            enum: [m, km]
        required: [attribute, units]        
