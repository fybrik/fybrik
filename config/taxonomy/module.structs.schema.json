{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$id": "module.schema.json",
    "title": "Module Taxonomy",
    "description": "Portion of the taxonomy contributed to by the mesh for data modules. Currently contains only what is needed by OPA.",
    "definitions": {
        "governance_decision": {
            "type": "object",
            "description": "Governance decision applied on the data.",
            "properties": {
                "name" : {"$ref": "module.values.schema.json#/definitions/governance_decision_name"},
				"params": {
                    "type": "array",
                    "description": "List of optional parameters provided to the transform function.",
                    "items": {
                        "type":"string"
                    }
                }
            },
            "required": ["name"]
        }
    },
    "properties": {
        "governance_decision": {"$ref": "#/definitions/governance_decision"}
    },
    "additionalProperties": false
}