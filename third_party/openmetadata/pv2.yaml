# If you want to use existing storage that you provisioned earlier, but never used in your cluster before, you must
# make the storage available in the same subnet as your worker nodes.
# See https://cloud.ibm.com/docs/openshift?topic=openshift-file_storage#existing-file-1

#apiVersion: v1
#kind: PersistentVolume
#metadata:
#  name: logs
#  labels:
#    app: logs
#spec:
#  capacity:
#    storage: 1Gi
#  accessModes:
#    - ReadWriteMany
#  claimRef: # the claimRef is here to provide a "connection" between this pv and the pvc.
#    apiVersion: v1
#    kind: PersistentVolumeClaim
#    name: logs
#    namespace: INSTALLATION_NAMESPACE
##  hostPath:
##    path: /tmp/
#---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs
  namespace: INSTALLATION_NAMESPACE
  labels:
    app: logs
spec:
        #storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      # The amount of the volume's storage to request
      storage: 1Gi
