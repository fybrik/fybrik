apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs
  labels:
    app: logs
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      # The amount of the volume's storage to request
      storage: 1Gi
